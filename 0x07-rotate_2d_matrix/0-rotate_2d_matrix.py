#!/usr/bin/python3
"""
Test 0x07 - Rotate 2D Matrix
"""

def rotate_2d_matrix(matrix):
    ligne = len(matrix)
    colonne = len(matrix[0])
    for line in matrix:
        for col in line:
            pass